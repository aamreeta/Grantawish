import java.net.UnknownHostException;




//import controllers.MorphiaObject;
/*import play.*;
import models.persistence.MongoPersistenceContext;

import org.mongodb.morphia.*;
import com.mongodb.MongoClient;



//import controllers.MorphiaObject;

public class Global extends GlobalSettings {
	public void onStart(Application app) {
		System.out.println("********Gloabl settings********"+app);
		super.beforeStart(app);
		Logger.debug("** onStart **"); 
		
		/*PersistenceContext.mongo = new MongoClient("127.0.0.1", 27017);
		
		PersistenceContext.morphia = new Morphia();
		PersistenceContext.datastore = PersistenceContext.morphia.createDatastore(PersistenceContext.mongo, "GrantAWishTest");
		PersistenceContext.datastore.ensureIndexes();   
		PersistenceContext.datastore.ensureCaps();  

		Logger.debug("** Morphia datastore: " + PersistenceContext.datastore.getDB());
		System.out.println("** Morphia datastore: " + PersistenceContext.datastore.getDB());
	  
	}
}*/
